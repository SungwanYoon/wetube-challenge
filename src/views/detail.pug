extends layouts/main.pug

block content
    a(href=`/${movie.id}/edit`) Edit
    h1= movie.title
    h3= movie.year
    h3= movie.rating
    h3= movie.synopsis
        ul
        each genre in movie.genres
            li=genre