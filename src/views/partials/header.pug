header.header
    .header__column
        a(href=routes.home) Calvin
        p= user.isAuthenticated
        p= user.id
    .header__column
        form(action=routes.search, method="get")
            input(type="text", placeholder="Search by term" name="term")
