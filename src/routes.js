const HOME = "/";
const LOGIN = "/login";
const PHOTOS = "/photos";
const PROFILE = "/profile";

const SEARCH = "/search";

const routes = {
  home: HOME,
  login: LOGIN,
  photos: PHOTOS,
  profile: PROFILE,

  search: SEARCH
};

export default routes;
